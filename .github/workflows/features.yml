name: Features

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    permissions:
      contents: read
    uses: ably/features/.github/workflows/sdk-features.yml@main
    with:
      repository-name: ably-flutter
    secrets: inherit
