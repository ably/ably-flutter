#import <Foundation/Foundation.h>

typedef NS_ENUM(UInt8, _Value) {

  {{#each types}}{{name}}CodecType = {{value}},
  {{/each}}
};


@interface AblyPlatformMethod : NSObject

{{#each methods}}@property (class, nonatomic, assign, readonly) NSString *{{name}};
{{/each}}
@end
